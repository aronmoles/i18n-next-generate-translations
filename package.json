{
  "name": "i18n-next-generate-translations",
  "version": "1.0.0",
  "author": "Aarón Molés",
  "license": "MIT",
  "main": "./lib/index.js",
  "bin": {
    "generate-translations": "./lib/index.js"
  },
  "scripts": {
    "start": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/index.ts",
    "create": "npm run build && npm run test",
    "build": "tsc -p .",
    "local": "sudo npm i -g && pizza",
    "refresh": "rm -rf ./node_modules ./package-lock.json && npm install"
  },
  "devDependencies": {
    "@types/node": "^14.14.9",
    "nodemon": "^2.0.6",
    "ts-node": "^9.0.0",
    "typescript": "^4.1.2"
  },
  "dependencies": {
    "commander": "^6.2.0",
    "xml2js": "^0.4.23"
  }
}
